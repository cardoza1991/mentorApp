# Application settings
appname = NEXUS Mentorship Platform
httpport = 8080
runmode = dev # Possible values: dev, prod, test

# Database configuration
db_host = localhost
db_port = 5432
db_user = postgres
db_password = postgres
db_name = nexus

# Session configuration
session_enabled = true
session_provider = postgresql
session_name = nexussessionid
session_gc_max_lifetime = 3600

# Security settings
enable_xsrf = false # Enable in production

